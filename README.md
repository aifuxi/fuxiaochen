# Fuxiaochen Project (FGO)

这是一个全栈应用项目，包含 Go 语言编写的高性能后端 API 服务，以及配套的后台管理系统和前台门户网站。

如果你在找之前的 Next.js + Prisma + MySQL 的全栈版本的项目代码，请切换到 `nextjs-fullstack` 分支。请参考 [nextjs-fullstack](https://github.com/aifuxi/fuxiaochen/tree/feat/nextjs-fullstack)。

## 🌟 项目简介

**本项目是一个个人学习技术和探索的项目，随时可能有 breaking change。不建议！！！不建议！！！不建议！！！用于生产环境，欢迎一起互相交流学习～**

本项目旨在提供一个现代化的全栈开发示例，涵盖了从后端服务到前端展示的完整流程。

- **后端 API**: 基于 Go (Gin) 开发，提供 RESTful API 服务。
- **后台管理 (Admin)**: 基于 React 19 + Semi UI 开发，提供内容管理、用户管理等功能。
- **门户网站 (Portal)**: 基于 Next.js 16 开发，提供高性能的 SSR/SSG 页面渲染，注重 SEO 和用户体验。

## 🛠 技术栈

### 后端 (Backend)

- **语言**: Go
- **Web 框架**: Gin
- **数据库**: MySQL
- **ORM**: GORM
- **鉴权**: JWT
- **配置**: Viper
- **日志**: Zap
- **存储**: 阿里云 OSS

### 前端 - 管理后台 (Admin)

- **框架**: React 19
- **构建工具**: Vite
- **UI 组件库**: Semi UI
- **状态管理**: Zustand
- **路由**: React Router v7
- **样式**: Tailwind CSS

### 前端 - 门户网站 (Portal)

- **框架**: Next.js 16 (App Router)
- **UI 组件库**: Shadcn UI + Radix UI
- **样式**: Tailwind CSS
- **图标**: Iconify

### 前端 - Monorepo 管理

- **包管理器**: pnpm
- **Workspace**: pnpm workspace

## 🚀 快速开始

### 环境要求

- Go 1.25+
- Node.js 20+
- MySQL 8.0+

### 1. 后端服务

请确保已配置好数据库连接信息（通常在 `config/app.yml` 中）。

```bash
# 运行数据库迁移
make migrate

# 启动开发服务器
make dev
```

### 2. 前端应用

前端项目位于 `web` 目录下，采用 pnpm monorepo 架构管理。请确保根目录下已安装 pnpm。

```bash
# 进入前端根目录
cd web

# 安装所有子项目依赖
pnpm install

# 启动管理后台 (fuxiaochen-admin)
pnpm dev:admin

# 启动门户网站 (fuxiaochen-portal)
pnpm dev:portal

# 构建项目
pnpm build:admin   # 构建 Admin
pnpm build:portal  # 构建 Portal
```

## 📂 目录结构

更多架构细节请参考 [AGENTS.md](./AGENTS.md)。

```
.
├── cmd/                # Go 程序入口
├── config/             # 后端配置
├── internal/           # 后端业务逻辑 (Handler, Service, Repo)
├── pkg/                # 公共工具包
├── web/                # 前端 Monorepo 根目录
│   ├── apps/           # 前端应用
│   │   ├── fuxiaochen-admin/  # React 管理后台
│   │   └── fuxiaochen-portal/ # Next.js 门户
│   ├── package.json    # Monorepo 依赖与脚本
│   └── pnpm-workspace.yaml # Workspace 定义
└── Makefile            # 构建命令
```

## 📄 许可证

[Apache License 2.0](LICENSE)
